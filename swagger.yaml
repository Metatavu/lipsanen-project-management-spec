openapi: 3.0.3
info:
  version: 1.0.0
  title: Lipsanen Project Management API

security:
  - bearerAuth: []

paths:
  /v1/system/ping:
    get:
      operationId: ping
      summary: Replies with pong
      description: Replies ping with pong
      tags:
        - System
      responses:
        "200":
          description: Pong
          content:
            text/plain:
              schema:
                type: string

  /v1/projects/import:
    post:
      operationId: importXml
      summary: Import XML file from Aikataulu
      description: Import XML file from Aikataulu
      tags:
        - Projects
      requestBody:
        content:
          application/xml:
            schema:
              type: string
              format: binary
      responses:
        "200":
          description: Imported project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects:
    get:
      operationId: listProjects
      summary: Get all projects
      description: Get all projects
      tags:
        - Projects
      parameters:
        - name: first
          in: query
          description: First result.
          schema:
            type: integer
        - name: max
          in: query
          description: Max results.
          schema:
            type: integer
      responses:
        "200":
          description: List of projects
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Project'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: createProject
      summary: Create a new project
      description: Create a new project
      tags:
        - Projects
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Project'
      responses:
        "200":
          description: Created project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}:
    get:
      operationId: findProject
      summary: Get a project
      description: Get a project
      tags:
        - Projects
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: updateProject
      summary: Update a project
      description: Update a project
      tags:
        - Projects
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Project'
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteProject
      summary: Delete a project
      description: Delete a project
      tags:
        - Projects
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: No content
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/export:
    get:
      operationId: exportProject
      summary: Exports project to Tocoman xml file
      description: Get a project
      tags:
        - Projects
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Project
          content:
            application/xml:
              schema:
                type: string
                format: binary
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/themes:
    get:
      operationId: listProjectThemes
      summary: Get all projects themes
      description: Get all projects themes, expected to be a list of 0 or 1 items
      tags:
        - ProjectThemes
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: List of Project Themes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProjectTheme'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: createProjectTheme
      summary: Create a new Project Theme
      description: Create a new Project Theme
      tags:
        - ProjectThemes
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectTheme'
      responses:
        "200":
          description: Created Project Theme
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTheme'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/themes/{themeId}:
    get:
      operationId: findProjectTheme
      summary: Get a project theme
      description: Get a project theme
      tags:
        - ProjectThemes
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: themeId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: ProjectTheme
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTheme'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: updateProjectTheme
      summary: Update a project theme
      description: Update a project theme
      tags:
        - ProjectThemes
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: themeId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectTheme'
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTheme'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteProjectTheme
      summary: Delete a project theme
      description: Delete a project theme
      tags:
        - ProjectThemes
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: themeId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: No content
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /v1/projects/{projectId}/milestones:
    get:
      operationId: listProjectMilestones
      summary: Get all project milestones
      description: Get all project milestones
      tags:
        - ProjectMilestones
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: List of project milestones
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Milestone'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: createProjectMilestone
      summary: Create a new project milestone
      description: Create a new project milestone
      tags:
        - ProjectMilestones
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Milestone'
      responses:
        "200":
          description: Created project milestone
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Milestone'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/milestones/{milestoneId}:
    get:
      operationId: findProjectMilestone
      summary: Get a project milestone
      description: Get a project milestone
      tags:
        - ProjectMilestones
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Project Milestone
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Milestone'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: updateProjectMilestone
      summary: Update a project milestone
      description: Update a project milestone
      tags:
        - ProjectMilestones
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Milestone'
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Milestone'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteProjectMilestone
      summary: Delete a project milestone
      description: Delete a project milestone
      tags:
        - ProjectMilestones
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: No content
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/milestones/{milestoneId}/tasks:
    get:
      operationId: listTasks
      summary: Get all milestone tasks
      description: Get all milestone tasks
      tags:
        - Tasks
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: first
          in: query
          description: First result.
          schema:
            type: integer
        - name: max
          in: query
          description: Max results.
          schema:
            type: integer
      responses:
        "200":
          description: List of project milestone tasks
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Task'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: createTask
      summary: Create a new milestone task
      description: Create a new milestone task
      tags:
        - Tasks
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Task'
      responses:
        "200":
          description: Created milestone task
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Task'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/milestones/{milestoneId}/tasks/{taskId}:
    get:
      operationId: findTask
      summary: Get a milestone task
      description: Get a milestone task
      tags:
        - Tasks
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Milestone Task
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Task'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: updateTask
      summary: Update a milestone task
      description: Update a milestone task
      tags:
        - Tasks
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Task'
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Task'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteTask
      summary: Delete a milestone task
      description: Delete a milestone task
      tags:
        - Tasks
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: No content
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/milestones/{milestoneId}/changeProposals:
    get:
      operationId: listChangeProposals
      summary: List change proposals within a milestone
      description: Lists change proposals within a milestone. Proposals are sorted from earliest to latest created.
      tags:
        - ChangeProposals
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: taskId
          in: query
          schema:
            type: string
            format: uuid
        - name: first
          in: query
          description: First result.
          schema:
            type: integer
        - name: max
          in: query
          description: Max results.
          schema:
            type: integer
      responses:
        "200":
          description: List of change proposals
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ChangeProposal'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: createChangeProposal
      summary: Create a new change proposal within a milestone
      description: Create a new change proposal within a milestone
      tags:
        - ChangeProposals
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangeProposal'
      responses:
        "200":
          description: Created change proposal
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangeProposal'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/milestones/{milestoneId}/changeProposals/{changeProposalId}:
    get:
      operationId: findChangeProposal
      summary: Find a specific change proposal within a milestone
      description: Find a specific change proposal within a milestone
      tags:
        - ChangeProposals
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: changeProposalId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Found change proposal
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangeProposal'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: updateChangeProposal
      summary: Update a specific change proposal within a milestone
      description: Update a specific change proposal within a milestone
      tags:
        - ChangeProposals
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: changeProposalId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangeProposal'
      responses:
        "200":
          description: Updated change proposal
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangeProposal'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteChangeProposal
      summary: Delete a specific change proposal within a milestone
      description: Deletes a specific change proposal within a milestone
      tags:
        - ChangeProposals
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: milestoneId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: changeProposalId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Change proposal deleted successfully
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/connections:
    get:
      operationId: listTaskConnections
      summary: Get all task connections
      description: Get all task connections
      tags:
        - TaskConnections
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: taskId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: connectionRole
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/TaskConnectionRole'
      responses:
        "200":
          description: List of task connections
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TaskConnection'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: createTaskConnection
      summary: Create a new task connection
      description: Create a new task connection
      tags:
        - TaskConnections
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskConnection'
      responses:
        "200":
          description: Created task connection
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskConnection'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/connections/{connectionId}:
    get:
      operationId: findTaskConnection
      summary: Get a task connection
      description: Get a task connection
      tags:
        - TaskConnections
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: connectionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Task Connection
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskConnection'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: updateTaskConnection
      summary: Update a milestone task
      description: Update a milestone task
      tags:
        - TaskConnections
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: connectionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskConnection'
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskConnection'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteTaskConnection
      summary: Delete a task connection
      description: Delete a task connection
      tags:
        - TaskConnections
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: connectionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: No content
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /v1/projects/{projectId}/notifications:
    get:
      operationId: listNotifications
      summary: Get all notifications
      description: Get all notifications
      tags:
        - Notifications
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: userId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: first
          in: query
          description: First result.
          schema:
            type: integer
        - name: max
          in: query
          description: Max results.
          schema:
            type: integer
        - name: readStatus
          in: query
          description: Read status of the notification filter
          schema:
            type: boolean
        - name: batchGrouping
          in: query
          description: Group notifications by batch if necessary to avoid multiple notifications for the same event but different users
          schema:
          type: boolean
      responses:
        "200":
          description: List of notifications
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Notification'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/projects/{projectId}/notifications/{notificationId}:
    get:
      operationId: findNotification
      summary: Get a notification
      description: Get a notification
      tags:
        - Notifications
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: notificationId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Notification
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: updateNotification
      summary: Update a notification
      description: Update a notification
      tags:
        - Notifications
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: notificationId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Notification'
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteNotification
      summary: Delete a notification
      description: Delete a notification
      tags:
        - Notifications
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: notificationId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: No content
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/users:
    get:
      operationId: listUsers
      summary: Get all users
      description: Get all users
      tags:
        - Users
      parameters:
        - name: companyId
          in: query
          description: Company id
          schema:
            type: string
            format: uuid
        - name: first
          in: query
          description: First result.
          schema:
            type: integer
        - name: max
          in: query
          description: Max results.
          schema:
            type: integer
        - name: includeRoles
          in: query
          description: Include roles in the resulting User objects, use only when necessary
          schema:
            type: boolean
      responses:
        "200":
          description: List of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: createUser
      summary: Create a new user
      description: Create a new user
      tags:
        - Users
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/User'
      responses:
        "200":
          description: Created user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/users/{userId}:
    get:
      operationId: findUser
      summary: Get a user
      description: Get a user
      tags:
        - Users
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: includeRoles
          in: query
          description: Include roles in response
          schema:
            type: boolean
      responses:
        "200":
          description: User
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: updateUser
      summary: Update a user
      description: Update a user
      tags:
        - Users
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/User'
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteUser
      summary: Delete a user
      description: Delete a user
      tags:
        - Users
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: No content
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /v1/companies:
    get:
      operationId: listCompanies
      summary: Get all companies
      description: Get all companies
      tags:
        - Companies
      parameters:
        - name: first
          in: query
          description: First result.
          schema:
            type: integer
        - name: max
          in: query
          description: Max results.
          schema:
            type: integer
      responses:
        "200":
          description: List of companies
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Company'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: createCompany
      summary: Create a new company
      description: Create a new company
      tags:
        - Companies
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Company'
      responses:
        "200":
          description: Created company
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Company'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/companies/{companyId}:
    get:
      operationId: findCompany
      summary: Get a company
      description: Get a company
      tags:
        - Companies
      parameters:
        - name: companyId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Company
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Company'
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteCompany
      summary: Delete a company
      description: Delete a company
      tags:
        - Companies
      parameters:
        - name: companyId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: No content
        "default":
          description: Invalid request was sent to the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"


components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    Error:
      type: object
      description: Error object
      required:
        - code
        - message
      properties:
        status:
          type: integer
          format: int32
        message:
          type: string

    ProjectStatus:
      type: string
      description: Project status
      enum:
        - INITIATION
        - PLANNING
        - DESIGN
        - PROCUREMENT
        - CONSTRUCTION
        - INSPECTION
        - COMPLETION

    Project:
      type: object
      description: Project object
      required:
        - name
        - status
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        tocomanId:
          type: integer
        status:
          $ref: '#/components/schemas/ProjectStatus'
        metadata:
          $ref: '#/components/schemas/Metadata'

    User:
      type: object
      description: User object
      required:
        - firstName
        - lastName
        - email
      properties:
        id:
          type: string
          format: uuid
        firstName:
          type: string
        lastName:
          type: string
        email:
          type: string
        companyId:
          format: uuid
          type: string
        lastLoggedIn:
          format: date-time
          type: string
        projectIds:
          description: List of project ids that the user belongs to
          type: array
          items:
            type: string
            format: uuid
        roles:
          description: User role
          readOnly: true
          type: array
          items:
            $ref: '#/components/schemas/UserRole'

    UserRole:
      type: string
      description: Role of the task in the connection
      enum:
        - ADMIN
        - USER

    Company:
      type: object
      description: Company object
      required:
        - name
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        metadata:
          $ref: '#/components/schemas/Metadata'

    ProjectTheme:
      type: object
      description: Theme object
      required:
        - themeColor
        - logoUrl
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        themeColor:
          type: string
          description: Theme color code
        logoUrl:
          type: string
          format: url
        metadata:
          $ref: '#/components/schemas/Metadata'

    Milestone:
      type: object
      description: milestone
      required:
        - name
        - startDate
        - endDate
        - originalStartDate
        - originalEndDate
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        name:
          type: string
        startDate:
          format: date
          type: string
        endDate:
          format: date
          type: string
        originalStartDate:
          format: date
          type: string
          description: Original start date of the milestone
        originalEndDate:
          format: date
          type: string
          description: Original end date of the milestone
        metadata:
          $ref: '#/components/schemas/Metadata'

    Task:
      type: object
      description: milestone
      required:
        - name
        - startDate
        - endDate
        - milestoneId
        - status
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        name:
          type: string
        startDate:
          format: date
          type: string
          description: Can be edited only within proposal OR in project planning stage by admin
        endDate:
          format: date
          type: string
          description: Can be edited only within proposal OR in project planning stage by admin
        milestoneId:
          type: string
          format: uuid
        status:
          $ref: '#/components/schemas/TaskStatus'
          description: Task status. TaskConnections is taken into account when updating the status
        assigneeIds:
          type: array
          items:
            type: string
            format: uuid
          description: List of assignee user IDs
        userRole:
          $ref: '#/components/schemas/UserRole'
          description: User role suggested for the task
        estimatedDuration:
          type: string
          description: Estimated duration of the task
        estimatedReadiness:
          type: string
          description: Estimated readiness of the task
        attachmentUrls:
          type: array
          items:
            type: string
            format: url
          description: URLs of attachments related to the task
        metadata:
          $ref: '#/components/schemas/Metadata'

    TaskStatus:
      type: string
      description: Task status
      enum:
        - NOT_STARTED
        - IN_PROGRESS
        - DONE

    ChangeProposal:
      type: object
      description: Change proposal
      required:
        - status
        - taskId
        - reason
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        taskId:
          type: string
          format: uuid
          readOnly: true
        startDate:
          description: Null if change is not needed and not null if the start date is proposed to change
          format: date
          type: string
        endDate:
          description: Null if change is not needed and not null if the end date is proposed to change
          format: date
          type: string
        reason:
          type: string
          description: The reason for the proposed change
        comment:
          type: string
          description: Additional comments or details about the proposed change
        status:
          $ref: '#/components/schemas/ChangeProposalStatus'
          description: Status of the change proposal
        metadata:
          $ref: '#/components/schemas/Metadata'

    ChangeProposalStatus:
      type: string
      description: Change proposal status
      enum:
        - PENDING
        - APPROVED
        - REJECTED


    TaskConnection:
      type: object
      description: Task connection
      required:
        - sourceTaskId
        - targetTaskId
        - type
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        sourceTaskId:
          type: string
          format: uuid
        targetTaskId:
          type: string
          format: uuid
        type:
          $ref: '#/components/schemas/TaskConnectionType'

    TaskConnectionRole:
      type: string
      description: Role of the task in the connection
      enum:
        - SOURCE
        - TARGET

    TaskConnectionType:
      type: string
      description: Task connection type
      enum:
        - FINISH_TO_START
        - START_TO_START
        - FINISH_TO_FINISH

    Notification:
      type: object
      description: Notification object that a user receives
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        notificationBatchId:
          description: ID of the notification batch that the notification belongs to
          type: string
          format: uuid
          readOnly: true
        userId:
          description: User ID that the notification belongs to
          type: string
          format: uuid
        message:
          description: notification message
          type: string
        read:
          description: Whether the notification has been read
          type: boolean
        type:
          $ref: '#/components/schemas/NotificationType'
        subjectId:
          description: ID of the subject entity of the notification, depends on the notification type
          type: string
          format: uuid
        metadata:
          $ref: '#/components/schemas/Metadata'

    NotificationType:
      type: string
      description: Notification type
      enum:
        - TASK_ASSIGNED
        - TASK_STATUS_CHANGED
        - CHANGE_PROPOSAL_STATUS_CHANGED
        - TASK_CONNECTION_CREATED
        - TASK_CONNECTION_DELETED
    Metadata:
      type: object
      description: Error object
      properties:
        creatorId:
          type: string
          readOnly: true
          format: uuid
        createdAt:
          format: date-time
          type: string
          readOnly: true
        lastModifierId:
          type: string
          readOnly: true
          format: uuid
        modifiedAt:
          format: date-time
          type: string
          readOnly: true